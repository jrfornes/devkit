# Visual baselines

PNG screenshots of known-good routes. `visual_diff` compares live renders against
these files.

| File | Route |
|------|-------|
| `home.png` | `/` |

Regenerate after intentional visual changes:

```bash
cd harness
npm run baseline:generate
```
